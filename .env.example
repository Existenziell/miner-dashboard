# Copy this file to .env and set your values.

# Your NerdQaxe++ miner's IP address (optional here — can also be set in Dashboard Settings)
MINER_IP=192.168.1.3

# Optional: GitHub Personal Access Token (no scope needed for public repos).
# Unauthenticated API requests are limited to 60/hour; a token raises the limit to 5,000/hour.
# Used when fetching firmware releases in Settings → Firmware.
# MINER_DASHBOARD_FIRMWARE=ghp_xxxxxxxxxxxx
